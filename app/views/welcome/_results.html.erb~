<ul class="list-unstyled">
  <% result['tlds'].each do |tld| %>
  <li>
    <!--<p class="<%= result['tld'] ? 'bg-danger' : 'bg-success' %>">-->
    <%= ".#{tld}" %>&nbsp;
    <%= render partial: "welcome/data", locals: {term: result['word'], taken: result[tld], tld: tld} %>
    <!--</p>-->
  </li>
  <% end %>
</ul>

